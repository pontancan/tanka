# 旅の短歌 — 引き継ぎメモ

旅先で同行者と五十音を順に使った短歌（57577）を詠み、旅ごとに1冊の共作句集を残すPWA。
代表者1台に入力する。データは端末のlocalStorageのみ。サーバーに送らない。

迷ったら削る側に倒す。

## 目的

**旅先で、その場にいる人と一緒に遊ぶこと。**

句集は遊んだ証拠であって、目的ではない。アプリは記録装置ではなく **盤**。
盤の仕事は、邪魔をしないこと。1台を回すこと自体が遊びの一部で、
渡す・覗き込む・横から口を出す、が起きている状態が正しい。

迷ったらここに照らす。「見立て」を疑っていいのか、「こうしたい」を守るべきなのかは、
目的からしか決まらない。理由の理由がここに置いてある。

~~**未決：** 2冊目を始めると1冊目が消える~~ → **2026-09-12 に解いた。**
三つの画面に分けて、しまった旅を棚に残すようにした（下の「詠む／読む／思い出」を見ること）。
遊ぶことが本体で、句集は遊んだ証拠 ── という読みは変えていない。
証拠が消えてしまうのが問題だっただけで、残るようになった今、目的の書き換えは要らない。

> この節は Claude がコードと過去の決定から逆算して書いた下書き（2026-09-02）。
> 松本の言葉で書き直すこと。目的だけは、AIの読みで固定してはいけない。

## 決定は3つの箱に分けてある

昔の決定が今の自分と合わなくなる。そこで **「どうなったら戻していいか」** で分けた。

| 箱 | 根拠 | 戻していい条件 |
|---|---|---|
| **わかったこと** | 実作・実測で確かめた | 根拠の実例が間違いだったと判明した時だけ。まず起きない |
| **こうしたい** | 意図。証拠では動かない | 松本が気を変えた時だけ。**AIが「改善」として触ってはいけない** |
| **今の見立て** | まだ確かめていない予測 | ここだけが棚卸しの対象。ドライランで確かめる |

新しく決めたことは、**迷ったら「見立て」に置く。** 決めたてのものを「わかったこと」に
混ぜるのが一番危ない（実際にやらかしている。下の「五つで一画面」を見ること）。

## 遊びのルール

- 1首＝五十音の1行。行の5音が各句の頭にくる（あ行〜ら行の9首で1冊）
- 出だしに困る難所（を・ん等）は最初から盤に無い
- 5・7・5・7・7。モーラで数える（じょ=1 / っ=1 / ん=1 / ー=1）
- 1首の5パーツを別々の人が作ってよい

## わかったこと（実作で確かめた。戻さない）

「親切な改善」で戻さないこと。根拠の実例が付いている。

- **頭の音をロックしない。** 実作に「ぶあついさしみ」（お題は「ふ」なのに濁って「ぶ」）がある。
  や行は い・え を借りる。だから⭕️は入力欄の外の *お題マーカー* で、入力は丸ごと自由
- **音数は表示するが、ぴったりで止めない。** 実作に8音の句が2つある
  （そのさきオアシス／よこみちはっぷん）。
  **上限は音数ではなく「入力欄の枠にはいるところまで」**（`scrollWidth > clientWidth` で止める）。
  5音の欄は幅が72%に絞ってあるので、枠そのものが長短を表している。
  玉と数字は `Math.max(haku, n)` で溢れても出る。画像の短冊も `nagasa` から高さを決めるので伸びる。
  → もとは `AMARI = 2` で +2音までだったが、それだと **根拠に挙げているその8音の実例が、
  5音の欄では7音に切られていた**（そのさきオアシ）。止める理由として書いた例を、
  アプリが消していた。音数で切ってはいけない（2026-09-02 実測）
- **濁点は同じ音とみなす**（ぶ→ふ で⭕️が灯る）。上と同じ事件から出ている
- **ページの横スクロールは止める**（縦書きの箱のスクロールがページに伝播していた）

下の「モバイル対応で踏んだ地雷」も、性質としてはこの箱に属する。全部実際に踏んだもの。

## こうしたい（意図。確かめる対象ではない）

証拠では動かない。**松本が気を変えた時だけ変わる。AIの判断で触らないこと。**

- **作者を記録しない。** みんなで作ることが本質。UIに「誰の番」を出さない
- **説明文を画面に置かない。** ⭕️が音を、玉が数を示している。伝わらないなら説明ではなく画面を直す
- **「保存しました」を出さない。** 自動保存はだまってやる。うまくいっている事を報告しない。
  ただし *保存できない* 時の警告（`kura.honmono` が false）は残す。あれは実害の知らせ
- **AIが手伝うのは素材（単語）まで。句そのものは作らせない。** ゲームが死ぬ

## 今の見立て（未検証。ドライランで棚卸しする）

**合わなくなるのはこの箱だけ。** 疑うならここを疑う。

- **完成の定義：2026/11/14 の旅で「詠む」画面が動き、1冊つくれること。**
  2026-09-01 に「旅が終わった後が抜けている」と揺れ、2026-09-12 に手当てした。
  いま無いのは **棚から消す手だて** と **別端末との同期**（同期は意図してやらない）

- **11/14までやらないこと：** 写真／地図／各自の端末での同期／作者記録／しおり読み込み／ストア配布

- **ドライラン以降は新機能を凍結。** 本番に触ったばかりの未検証コードを持ち込まないため

- **「よむ」は五つの短冊でちょうど一画面。**（2026-09-01）
  `#uta` が `100svh`、`space-evenly` で上下と間に均等に散らす。`gap` と `padding` を
  同じ14pxにしてあるので六つの空きが等しくなる。実測で SE3rd 35px ／ iPhone15 53px ／
  Pixel 60px ／ 16ProMax 68px と自分で釣り合う（機種ごとの分岐が要らない）。
  **画面を勝手に送らない。** 行をえらんだ時に `scrollIntoView` で五つの先頭へ飛ばしたら、
  行を変えるたびに一首目へ引き戻されて邪魔だった。遊びがある方がいい。
  → **実機の実測はあるが、旅で詠んで確かめてはいない。** ここに置いてあるのはそのため。
  直すなら丸ごと考え直すこと。**部分的に固定の余白へ戻すのが一番悪い**（機種の分岐が復活する）。
  **下の 45vh の遊びは消さないこと。** 一度消して酔った（地雷の節を見ること）

- **日付は複数日程。指でなぞってえらぶ。**（2026-09-12）
  `type="date"` は範囲を持てないので、自作の暦に差し替えた。
  一日なら触る、何日かにわたるならなぞる。なぞれない時も二度触れば範囲になる
  （キーボードの Enter も拾う）。
  **前後の月の日も薄く出して、えらべるようにする。** 出さないとなぞりが月の端で止まり、
  月をまたぐ旅が作れない。二度触りでも跨げるが、それは画面から読み取れないので当てにしない。
  頭の週はもう一週さかのぼる（`getDay() + 7`、49マス）。そうしないと **日曜はじまりの月**
  （2026年11月など）で前の月が一日も出ず、後ろへは跨げるのに前へは跨げなかった。
  どの月でも前7日以上・後4日以上が出る。
  **押した時点では決めず、離す時に「触っただけ」か「なぞった」かを見る。**
  押した瞬間に確定させたら、直前に触った日が起点のまま残り、別の日をなぞると
  範囲が繋がった（3日を触ってから14→17をなぞると 3–17 になる）。
  なぞり中かどうかは **自分で持つ**。`hasPointerCapture` で判定したら、
  捕まえ損ねた時になぞりが静かに無効になっていた。
  データは `dateEnd` を足しただけで `v` は 1 のまま。
  古い保存（`date` だけ）は一日の旅としてそのまま読める。
  表示は同じ月なら日だけ、同じ年なら月日だけ添える（2026.11.14–16／2026.11.28–12.02）。
  → **実機の指では試していない。** 合成のポインタ事象で通したところまで

- **詠む／読む／思い出 の三画面。**（2026-09-12）
  詠む＝作る／読む＝今の旅を見る／思い出＝しまった旅の棚。
  詠む と 読む は声に出すと同じだが、字で見れば区別でき、対になっているのを採った。
  **昔の旅は見るだけ。書き換えられない**（今詠んでいる旅を踏む事故が起きないように）。
  - **しまう**：同じ旅なら `id` で上書きするので、何度しまってもふえない
  - **まっさらにする**：二度押さないと消えない（`kakunin`、4秒で取り消しが解ける）。
    取り消せないことにだけ使う。`confirm()` を使わないのは、画面の調子を保つため
  - 今の冊は `tabi-tanka:satsu`、棚は `tabi-tanka:omoide` に別で持つ。
    `v` は 1 のままで `id` を足しただけなので、**今ある保存はそのまま読める**
  - 表紙と札（`kazaru`）・画像（`egaku`）・文字（`mojiban`）は冊を引数で受け取る。
    `satsu` を直に見ていると昔の旅が描けない
  - **棚から消す手だてはまだ無い。** 要るかどうかはドライランで

- **改行で次の音へ。**（2026-09-12）五つめでは焦点を外してキーボードを閉じる。
  返しキーの顔つきも `enterKeyHint` で next / done に変える。
  変換中の Enter を拾わないことが要（地雷の節を見ること）

- **画像は見本を出してから保存する。**（2026-09-12）
  「画像にする」が即ダウンロードで、出来上がりを見られなかった。
  描くところ（`egaku`）と保存するところを分けて、まず見本を画面に出す。
  句が変わったら `hon()` で古い見本を引っこめる。
  iOSでは `a.download` が素直に効かないことがあるが、画面に出ていれば長押しでも保存できる

- **おもいでも出す行をえらべる。**（2026-09-12）
  よむと同じ行えらびを置くが、こちらは複数えらべる（`aria-current` ではなく `aria-pressed`）。
  はじめは書いた行が全部オン。押すと消える。一首しか無いならえらぶ意味がないので出さない。
  **文字も画像も `miruUta()` を見る。出しているものが、そのまま出る。**
  見本が先に出るので、何が保存されるかは押す前に分かる。
  → 表示と書き出しを別々にする手もあったが、規則が二つに割れる。一つにした。
  消している行は **しまわない**（画面のことなので、開き直せば全部出る）

- **おもいでの札にも行の印（落款）。**（2026-09-12）
  書き出した画像には入るのに、画面ではどの行の首か分からなかった。
  親が縦書きなので印だけ `horizontal-tb` に戻す。
  **`.tate` の左のあき（`padding-left:52px`）は落款の場所。** 詰めると長い句で字と印が当たる
  （画像側の `hidari = 34` と同じ役目）

## モバイル対応で踏んだ地雷

- 入力欄は16px以上（未満だとiOSが勝手に拡大する）
- フォーカス時に `scrollIntoView`（キーボードで隠れる）。ただし **隠れている時だけ**。
  `visualViewport` で実際に隠れているか見る。見えている欄を動かすと酔う
- **画面の下に遊びを残す**（`#men-yomu` の `padding-bottom:45vh`）。
  一度これを消して「五つでピッタリ、下にスクロールの余地ゼロ」にしたら、
  キーボードが上がった時に最後の欄を持ち上げられず、ブラウザが独自に画面をずらし、
  「完了」で引き戻した。速くて酔う。**常に在ること**が大事（焦点の出入りで
  余白を付け外しすると、そこでまた跳ねる）
- ~~日付欄は `::-webkit-date-and-time-value` で左寄せ~~ → ネイティブの `type="date"` は
  もう使っていない（自作の暦に差し替えた）。また日付欄を足すなら、Safariが中央寄せにするのを思い出すこと
- **なぞる面には `touch-action:none`。** 無いとiOSでは指の動きがページのスクロールに食われる
- IME変換中は文字数を切らない（`compositionstart` / `compositionend` で制御）
- **`satsu` を丸ごと入れ替えたら `shirabeNaosu()` を呼ぶ。** 旅の名前・いっしょによむひとの欄は
  起動時に一度値を入れるだけなので、入れ替えても欄は前のまま残る。
  しかも `input` が `satsu[key] = el.value` なので、**一文字打った拍子に古い名が復活する**
  （まっさらにしたのに「熱海の旅あ」になった）。暦は `tsukiNi()` が引き直すので無事
- **変換中の Enter は「確定」であって「改行」ではない。** 取り違えると、変換を確定した拍子に
  次の欄へ飛ぶ。`henkanchu` / `e.isComposing` / `e.keyCode === 229` の三つで弾く

## ファイル

- `index.html` — 全部入り。外部ライブラリなし（旅先の圏外でも動く）
- `sw.js` — オフライン用。本体は常に新しい方を先に取りに行く（更新が貼りつかないように）
- `manifest.webmanifest` / `icon-*.png` — ホーム画面用。アイコンは藍地に白の「あ」

## デプロイ

GitHub Pages。<https://pontancan.github.io/tanka/>
リポジトリは <https://github.com/pontancan/tanka>（public、main ブランチのルートを配信）。

**push すれば公開される。** それだけ。

```
git add -A && git commit -m "..." && git push
```

反映まで1〜2分。`gh api repos/pontancan/tanka/pages --jq .status` が `built` になれば出ている。

Netlifyにしなかった理由：CLIが入っておらず、ログインでブラウザが開く。
`gh` は認証済みだったので、リモートから完結する方を選んだ。
パスは全部相対なので、サブパス配信（/tanka/）でも壊れない。この相対パスは崩さないこと。

## 残っていること

- ネタ袋（旅先で拾った言葉を溜めて、頭の音で引く）— MVP最後の1つ
- 旅の前のドライラン（近場で模擬1冊）
  → ここが **見立ての箱の棚卸しどき**。予測が実作に変わる。凍結の線もここで引く
